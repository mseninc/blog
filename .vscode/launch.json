{
  "configurations": [
    {
      "name": "lint",
      "request": "launch",
      "type": "node-terminal",
      "command": "npx textlint ${file} & node .github/scripts/lint/lint.js ${file}",
      "cwd": "${workspaceFolder}"
    }
  ]
}
