{
  "configurations": [
    {
      "name": "textlint",
      "request": "launch",
      "type": "node-terminal",
      "command": "npx textlint ${file}",
      "cwd": "${workspaceFolder}"
    }
  
  ]
}
